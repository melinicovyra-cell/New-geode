name: Build Geode Mod

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build placeholder
        run: |
          echo "Building Geode mod..."
          ls -la
          echo "Done"
